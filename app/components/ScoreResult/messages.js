/*
 * HomePage Messages
 *
 * This contains all the text for the HomePage component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.components.User';

export default defineMessages({
  scoreSummary: {
    id: `${scope}.User`,
    defaultMessage: 'Score summary',
  },
});
